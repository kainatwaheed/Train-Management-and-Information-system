<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
      <% @train.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :noofseats %><br>
    <%= f.number_field :noofseats %>
  </div>
  <div class="field">
    <%= f.label :noofclasses %><br>
    <%= f.number_field :noofclasses %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
